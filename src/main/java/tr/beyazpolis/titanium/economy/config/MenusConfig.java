package tr.beyazpolis.titanium.economy.config;

import org.bukkit.plugin.Plugin;

public class MenusConfig extends Config{

  public MenusConfig(final Plugin plugin, final String ymlName) {
    super(plugin, ymlName);
  }
}
